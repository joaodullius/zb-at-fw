/*
 * ZBOSS configurable RAM globals in their own translation unit, compiled
 * with -fno-lto (see CMakeLists.txt), as in the NCS Zigbee coordinator sample.
 */
#include <zboss_api.h>
#include <zb_mem_config_max.h>
